# Production dependencies — intentionally tiny for serverless (~100MB).
# All Gemini calls (chat, embeddings, transcription) use plain REST.
Flask>=3.0.0
gunicorn>=21.2.0
python-dotenv>=1.0.0
pymongo>=4.5.0
PyJWT>=2.8.0
requests>=2.31.0
numpy>=1.26.0

# Dev/testing only:
mongomock>=4.1.2
